# docker-compose build (create images)
#docker-compose run --rm app (app is the name of Docker images in this project , --rm means remove the container. Then you have to run this command bellow)
# docker-compose up